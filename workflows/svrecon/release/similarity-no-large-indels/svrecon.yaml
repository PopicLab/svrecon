# Positive callset, scored with: bulk error + no indel >= max_indel_size.
# Run from this directory. Outputs (svrecon.log) land here.

reference: ../../../data/chr21.fa
calls: ../../../insilicoSV/release/sim.vcf
sample: ../../../insilicoSV/release/sim.hapA.fa

buffer: 500
match_error_threshold: 0.1
max_indel_size: 50
verbose: true
plot_first_n: 0
