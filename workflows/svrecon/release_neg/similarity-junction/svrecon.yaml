# Negative callset, scored with: bulk error + per-breakpoint local error within junction_radius.
# Run from this directory. Outputs (svrecon.log) land here.

reference: ../../../data/chr21.fa
calls: ../../../insilicoSV/release_neg/sim.vcf
sample: ../../../insilicoSV/release/sim.hapA.fa

buffer: 500
match_error_threshold: 0.1
junction_radius: 100
verbose: true
plot_first_n: 0
